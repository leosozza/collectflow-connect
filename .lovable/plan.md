
Reorganizar a Linha 1 para ocupar melhor o espaço horizontal:

- Voltar `ml-auto` simples (remove `mr-auto`) — cluster de ações volta para a direita.
- Dar ao `<h1>` do nome `flex-1 min-w-0 truncate` para ocupar o espaço entre o botão voltar e o cluster, evitando o "vácuo" central.
- Manter botões + Em Aberto empilhados à direita.

Arquivo: `src/components/client-detail/ClientDetailHeader.tsx` (linhas 434-435).
